const AuthLayout = ({ children }) => {
  return (
    <div className="min-h-screen flex justify-center items-center w-full max-w-sm mx-auto">
      {children}
    </div>
  )
}

export default AuthLayout
